# Cliente web
Se encarga de disponibilizar el frontar que consumirá el usuario cuando realice consultas sobre las transcripciones de las llamadas de los clientes.

## Paquetes
```
pip install agent-framework-ag-ui --pre
pip install streamlit
pip install httpx
```

## Comando para ejecutar
```
streamlit run .\client.py
```